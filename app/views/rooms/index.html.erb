<h2>房間列表</h2>
<% @rooms = [] %>
<!--
<table>
  <thead>
    <tr>
      <th>類型</th>
      <th>房間號碼</th>
      <th>價格</th>
      <th>預約狀態</th>
      <% if user_signed_in? %><th>歷史預約紀錄</th><% end %>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.room_type.room_type_zh %></td>
        <td><%= room.room_number %></td>
        <td><%= room.price %></td>
        <td>
        <% if user_signed_in? %>
          <%= room.reservation_number ? link_to('已預約', user_booking_record_path(current_user, room.reservation_number)) : link_to('預約', reservation_hotel_room_path(@hotel, room)) %>
        <% else %>
          <%= room.reservation_number ? '已額滿' : link_to('可預約', reservation_hotel_room_path(@hotel, room)) %>
        <% end %>
        </td>
        <% if user_signed_in? %>
          <td>
            <%= room.booking_records.count > 0 ? link_to('預約記錄', reservation_hotel_room_path(@hotel, room)) : '無紀錄' %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
-->
<table>
  <thead>
    <tr>
      <th>房間類型</th>
      <th>房間價格</th>
      <th>容許人數</th>
    </tr>
  </thead>
  <tbody>
    <% @roomtyps.each do |room_type| %>
      <tr>
        <td><%= room_type.room_type_zh %></td>
        <td>200</td>
        <td>1</td>
      </tr>
    <% end %>
  </tbody>
</table>